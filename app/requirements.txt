# Pinned to keep the Ray Client (driver) and RayCluster versions in lockstep.
# This image is used for BOTH the controller and the Ray head/worker pods, so
# there is a single Ray version across driver and cluster.
ray[client,default]==2.40.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
numpy==2.1.3
pillow==11.0.0
kubernetes==31.0.0
